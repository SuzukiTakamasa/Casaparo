{
  "name": "line-bot-server",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "deploy-prod": "wrangler deploy --config wrangler.prod.toml",
		"deploy-dev": "wrangler deploy --config wrangler.dev.toml",
    "start": "wrangler dev",
    "test": "npx wrangler dev --test-scheduled"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240208.0",
    "typescript": "^5.0.4",
    "wrangler": "^3.0.0"
  }
}
