{
	"name": "template-worker-rust",
	"version": "0.0.0",
	"private": true,
	"scripts": {
		"deploy": "npx wrangler deploy",
		"dev": "wrangler dev",
		"build": "wasm-pack build --target web"
	},
	"devDependencies": {
		"wrangler": "^3.1.2"
	}
}
